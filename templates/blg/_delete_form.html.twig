<form method="post" action="{{ path('app_blg_delete', {'idB': blg.idB}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blg.idB) }}">
    <button class="btn">Delete</button>
</form>
