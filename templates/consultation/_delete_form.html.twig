<form method="post" action="{{ path('app_consultation_delete', {'idC': consultation.idC}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consultation.idC) }}">
    <button class="btn">Delete</button>
</form>
