<form method="post" action="{{ path('app_coaching_delete', {'id': coaching.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coaching.id) }}">
    <button class="btn">Delete</button>
</form>
